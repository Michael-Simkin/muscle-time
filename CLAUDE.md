Follow `@AGENTS.md`. It is source of truth.

Use `.claude/skills/*` for feature/refactor/release workflows.
Use `/grill-with-docs` for plan/design grilling that should also maintain project glossary and ADRs. It depends on `/grilling` and `/domain-modeling`.

Hooks enforce safety and formatting. Do not bypass hooks.
