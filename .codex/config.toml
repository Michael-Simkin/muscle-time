model = "gpt-5-codex"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[profiles.plan]
model = "gpt-5.5"
approval_policy = "on-request"
sandbox_mode = "read-only"

[profiles.implement]
model = "gpt-5-codex"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
